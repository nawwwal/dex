# Embedded Deck Context

The user wants a small Reveal.js deck inside a documentation page.

Constraints:

- The host page owns the URL hash and browser history.
- Keyboard shortcuts should work only when the deck is focused.
- The deck should not steal page scroll.
- The deck should have lower motion than a live stage talk.
- The output must include the configuration tradeoff.
