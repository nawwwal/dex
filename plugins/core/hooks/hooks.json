{
  "hooks": {
    "UserPromptSubmit": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "/bin/sh -c 'if [ -n \"${CLAUDE_PLUGIN_ROOT:-}\" ]; then exec /usr/bin/python3 \"$CLAUDE_PLUGIN_ROOT/hooks/portent_context_receipt.py\"; elif [ -n \"${PLUGIN_ROOT:-}\" ]; then exec /usr/bin/python3 \"$PLUGIN_ROOT/hooks/portent_context_receipt.py\"; else exit 0; fi'",
            "timeout": 5,
            "statusMessage": "Checking Portent context"
          }
        ]
      }
    ]
  }
}
